<%= form_for(current_user.requested_relationships.build, remote: true) do |f| %>
	<%= hidden_field_tag :friend_pasive_id, user.id %>
	<button type="submit" name="commit" class ="friendship_button">Send friendship request</button>
<% end %>