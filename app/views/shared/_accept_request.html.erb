<%= form_for(current_user.received_relationships.find_by(friend_active_id: user.id),html:{ method: :patch} ,remote: true) do |f| %>
	<button type="submit" name="commit" class ="friendship_button">Accept friendship request</button>
<% end %>