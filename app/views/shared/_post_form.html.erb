<%= form_with(model: @post) do |f| %>
	
		<%= f.text_area :content, placeholder: "post something" %>

		<br>

		<%= f.label " Upload Image or video" %>
		<%= f.file_field :media %>  

		<br>

		<%= f.submit "Post" %>
		

<% end %>