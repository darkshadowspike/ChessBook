button = document.querySelector("#friendship_button");

while (button.firstChild) {
	button.removeChild(button.firstChild);
}

button.insertAdjacentHTML("afterbegin", "<%= j render 'shared/friendship_form' %>");
